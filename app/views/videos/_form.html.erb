<%= form_for @video, :html => { :class => "form-horizontal video" } do |f| %>

    <% if @video.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@video.errors.count, "error") %> prohibited this video from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @video.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :v_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :v_id, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:v_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:title]) %>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:description]) %>
  </div>
  <div class="control-group">
    <%= f.label :published_at, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :published_at, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:published_at]) %>
  </div>
  <div class="control-group">
    <%= f.label :thumbnail_url, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :thumbnail_url, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:thumbnail_url]) %>
  </div>
  <div class="control-group">
    <%= f.label :channel_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :channel_id, :class => 'form-control' %>
    </div>
    <%= error_span(@video[:channel_id]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            videos_path, :class => 'btn btn-default' %>

<% end %>
